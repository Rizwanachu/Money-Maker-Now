openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: ProposAI API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: proposals
    description: Proposal management
  - name: dashboard
    description: Dashboard and analytics
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /proposals:
    get:
      operationId: listProposals
      tags: [proposals]
      summary: List all proposals for the authenticated user
      parameters:
        - in: query
          name: status
          schema:
            type: string
            enum: [draft, sent, won, lost]
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
        - in: query
          name: offset
          schema:
            type: integer
            default: 0
      responses:
        "200":
          description: List of proposals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProposalList"
        "401":
          description: Unauthorized
    post:
      operationId: createProposal
      tags: [proposals]
      summary: Create a new proposal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProposalBody"
      responses:
        "201":
          description: Proposal created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Proposal"
        "401":
          description: Unauthorized

  /proposals/{id}:
    get:
      operationId: getProposal
      tags: [proposals]
      summary: Get a proposal by ID
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Proposal details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Proposal"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    put:
      operationId: updateProposal
      tags: [proposals]
      summary: Update a proposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProposalBody"
      responses:
        "200":
          description: Proposal updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Proposal"
        "401":
          description: Unauthorized
        "404":
          description: Not found
    delete:
      operationId: deleteProposal
      tags: [proposals]
      summary: Delete a proposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /proposals/{id}/generate:
    post:
      operationId: generateProposalContent
      tags: [proposals]
      summary: Generate AI content for all 7 proposal sections (streams SSE)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Generated content (streamed as text/event-stream)
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /proposals/{id}/status:
    patch:
      operationId: updateProposalStatus
      tags: [proposals]
      summary: Update proposal status
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProposalStatusBody"
      responses:
        "200":
          description: Status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Proposal"
        "401":
          description: Unauthorized
        "404":
          description: Not found

  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [dashboard]
      summary: Get dashboard statistics
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"
        "401":
          description: Unauthorized

  /dashboard/recent:
    get:
      operationId: getRecentProposals
      tags: [dashboard]
      summary: Get recent proposals for dashboard feed
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            default: 5
      responses:
        "200":
          description: Recent proposals
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Proposal"
        "401":
          description: Unauthorized

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    ProposalStatus:
      type: string
      enum: [draft, sent, won, lost]

    ProposalNiche:
      type: string
      enum: ["Web Design", "Marketing Agency"]

    Proposal:
      type: object
      properties:
        id:
          type: integer
        userId:
          type: string
        clientName:
          type: string
        clientBrief:
          type: string
        niche:
          $ref: "#/components/schemas/ProposalNiche"
        status:
          $ref: "#/components/schemas/ProposalStatus"
        dealValue:
          type: string
          nullable: true
        executiveSummary:
          type: string
          nullable: true
        understanding:
          type: string
          nullable: true
        approach:
          type: string
          nullable: true
        timelinePlan:
          type: string
          nullable: true
        investment:
          type: string
          nullable: true
        whyUs:
          type: string
          nullable: true
        nextSteps:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        - id
        - userId
        - clientName
        - clientBrief
        - niche
        - status
        - createdAt
        - updatedAt

    ProposalList:
      type: object
      properties:
        proposals:
          type: array
          items:
            $ref: "#/components/schemas/Proposal"
        total:
          type: integer
      required:
        - proposals
        - total

    CreateProposalBody:
      type: object
      properties:
        clientName:
          type: string
        clientBrief:
          type: string
        niche:
          $ref: "#/components/schemas/ProposalNiche"
      required:
        - clientName
        - clientBrief
        - niche

    UpdateProposalBody:
      type: object
      properties:
        clientName:
          type: string
        clientBrief:
          type: string
        niche:
          $ref: "#/components/schemas/ProposalNiche"
        dealValue:
          type: string
          nullable: true
        executiveSummary:
          type: string
          nullable: true
        understanding:
          type: string
          nullable: true
        approach:
          type: string
          nullable: true
        timelinePlan:
          type: string
          nullable: true
        investment:
          type: string
          nullable: true
        whyUs:
          type: string
          nullable: true
        nextSteps:
          type: string
          nullable: true

    UpdateProposalStatusBody:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/ProposalStatus"
        dealValue:
          type: string
          nullable: true
      required:
        - status

    DashboardStats:
      type: object
      properties:
        totalProposals:
          type: integer
        draftCount:
          type: integer
        sentCount:
          type: integer
        wonCount:
          type: integer
        lostCount:
          type: integer
        winRate:
          type: number
          format: float
      required:
        - totalProposals
        - draftCount
        - sentCount
        - wonCount
        - lostCount
        - winRate
